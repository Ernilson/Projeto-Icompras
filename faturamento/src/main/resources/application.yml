spring:
  application:
    name: faturamento
  kafka:
    boostrap-server: 'http://Localhost:29092'
    consumer:
      group-id: icompras-faturamento
      auto-offset-reset: earliest
icompras:
  config:
    kafka:
      topics:
        pedidos-pagos: 'icompras.pedidos-pagos'


server:
  port: 8083

minio:
  url: 'http://Localhost:9000'
  access-key: minioadmin
  secret-key: minioadmin123
  bucket-name: faturas
